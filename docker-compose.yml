services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: whitecat-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Optional: Mount for logs if needed
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
